<div>
    <a href="/board">글 목록</a>
    <a href="/board/<%= result.id%>">돌아가기</a>
</div>
<h2>글 수정</h2>
<div>
    <form id="Form" action="/board/<%= result.id%>" method="post">
        <div>
            <label for="title">Title</label>
            <input type="text" value="<%= result.title%>" id="title" name="title">
        </div>
        <div>
            <label for="content">Content</label>
            <textarea id="content" name="content"><%= result.content%></textarea>
        </div>
        <button type="submit">Submit</button>
    </form>
</div>